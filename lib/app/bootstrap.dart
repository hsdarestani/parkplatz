import 'package:flutter/material.dart';
import 'app.dart';
class FreiraumBootstrap extends StatelessWidget { const FreiraumBootstrap({super.key}); @override Widget build(BuildContext context)=>const FreiraumApp(); }
